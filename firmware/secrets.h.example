#pragma once
/*
 * Smart Glass Canary -- credentials.
 *
 * THIS FILE IS NOT SAFE TO COMMIT. It is listed in .gitignore; keep it that
 * way. Copy this file to secrets.h and fill in your own values.
 */

#define WIFI_SSID              "YOUR_SSID"
#define WIFI_PASS              "YOUR_PASSWORD"


// Broker address, e.g. "192.168.1.50". Leave empty to disable MQTT entirely.
#define MQTT_HOST              ""

// Broker credentials. Leave both empty for an anonymous broker.
#define MQTT_USER              ""
#define MQTT_PASS              ""
